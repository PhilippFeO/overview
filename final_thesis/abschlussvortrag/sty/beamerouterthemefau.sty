\setbeamertemplate{frametitle}{%
	\vskip8.4mm%
	%
	\begin{minipage}[c][30.2mm][t]{\textwidth}
		\ifx\insertframesubtitle\empty
		\insertframetitle\vphantom{Äg}
		\else
		\insertframetitle\vphantom{Äg}\\[-0.4ex]
		{\usebeamerfont{framesubtitle}{\hskip0.2mm\insertframesubtitle\vphantom{Äg}}}
		\fi
	\end{minipage}
}

\setbeamertemplate{headline}{
  \setlength{\unitlength}{1mm}
  %
  \begin{tikzpicture}[%
      x=\unitlength,%
      y=\unitlength,%
      outer sep=0,%
      inner sep=0,%
      remember picture, overlay,%
      shift={(current page.south west)},%
    ]
    %
    % horizontal bar top
    \coordinate (b1) at (\the\marginleft-0.4mm,\the\paperheight-33.2mm);
    \draw[line width=2mm, draw=maincolor] (b1) to (b1 -| current page.north east);
    %
    % Internal logo
    \node[anchor=north east] at (\the\paperwidth/1.25,\the\paperheight-6mm)
      {\pgfuseimage{logo_large}};
    % FAU logo
    \node[anchor=north east] at (\the\paperwidth-\the\marginright,\the\paperheight-8mm)
      {\pgfuseimage{fau_small}};
  \end{tikzpicture}
}

\setbeamertemplate{footline}{%
  \setlength{\unitlength}{1mm}
  %
  % the picture environment is used to reserve some space (important for footnotes)
  \begin{picture}(254,13.3)(0,0)
    \begin{tikzpicture}[%
        x=\unitlength,%
        y=\unitlength,%
        outer sep=0,%
        inner sep=0,%
        remember picture, overlay,%
        shift={(current page.south west)},%
      ]
      %
      % horizontal bar bottom
      \coordinate (b1) at (\the\marginleft-0.4mm,8.9mm);
      \draw[line width=0.5mm, draw=lightcolor] (b1) to (b1 -| current page.north east);
      %
      % author and title (left hand side)
      \node[anchor=west, align=left] at (\the\marginleft,4.45mm)
        {%
          \usebeamercolor[fg]{footer}
          \usebeamerfont{footer}{%
            \ifx\OptLogoSmall\empty\else
              \tikz[baseline=-1mm, outer sep=0, inner sep=0]{\node[]{\pgfuseimage{logo_small}};}
              %
              \hskip1.2mm%
            \fi
            %
            \ifx\insertinstitute\empty\else%
              \insertshortinstitute\;\,$\mathbin{\vcenter{\hbox{\rule{.5ex}{.5ex}}}}$\;\,%
            \fi%
            %
            \insertshorttitle%
            %
            \ifx\insertauthor\empty\else%
              \;\,$\mathbin{\vcenter{\hbox{\rule{.5ex}{.5ex}}}}$\;\,\insertshortauthor%
            \fi%
          }
        };
      %
      % date and page (right hand side)
      \node[anchor=east, align=left] at (\the\paperwidth-\the\marginright,4.45mm)
        {%
          \usebeamercolor[fg]{footer}
          \usebeamerfont{footer}{%
            %
            \ifx\insertdate\empty\else%
              ~\insertshortdate\quad\quad\quad%
            \fi%
            %
            \insertframenumber~/~\inserttotalframenumber
          }
        };
    \end{tikzpicture}
  \end{picture}
}
